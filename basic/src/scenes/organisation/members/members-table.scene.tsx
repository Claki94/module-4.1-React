import { AppLayout } from "@/layout";
import { MembersTableContainer } from "@/pods/organisation";
import React from "react";

export const MembersTableScene: React.FC = () => (
  <AppLayout>
    <MembersTableContainer />
  </AppLayout>
);
