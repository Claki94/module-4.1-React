import { AppLayout } from "@/layout";
import { OrganisationMembersContainer } from "@/pods/organisation-members";
import React from "react";

export const OrganisationMembersScene: React.FC = () => (
  <AppLayout>
    <OrganisationMembersContainer />
  </AppLayout>
);
